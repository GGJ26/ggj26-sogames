using UnityEngine;
using System.Collections.Generic;

public class Soundpooling : MonoBehaviour
{ /*
    [System.Serializable] 
public class SoundEffect
    {
        public string name;
        public AudioClip clip;
        [Range (0f, 1f)] public float volume = 1f;
        [Range(0f, 1f)] public float pitch = 1f;
    }
    [SerializeField] private SoundEffect[] soundEffects;
    [SerializeField] private int poolSize = 8;
   
    private List<AudioSource> audioSources = new List<AudioSource>();
    private Dictionary<string, AudioClip> soundDictionnary = new Dictionary<string, AudioClip>();
    private float globalClipVolume = 1f;

    // ici on peut mettre le singleton de l'instanciation de l'audiomanager

    for (int i = 0; i < poolSize; i++)
    */
}
    
    


